# รายงานผลการศึกษาและข้อเสนอเชิงระบบ  
## การพัฒนาแผนอัตรากำลังบุคลากรแบบองค์รวม (Human Resources Blueprint: HR Blueprint) เขตสุขภาพที่ 1

วันที่จัดทำ: 4 พฤษภาคม 2569 (2026-05-04)  
ขอบเขตพื้นที่: 8 จังหวัด เขตสุขภาพที่ 1 (เชียงใหม่, เชียงราย, ลำปาง, น่าน, พะเยา, ลำพูน, แพร่, แม่ฮ่องสอน)

เอกสารฉบับนี้จัดทำเพื่อ “สรุปผลการศึกษา ณ ปัจจุบัน” ให้สั่งการได้ในระดับทิศทาง โดยใช้ข้อมูลจริงที่มีอยู่ในโปรเจค + การคำนวณ baseline/ช่องว่าง (gap) แบบรายจังหวัดก่อน เมื่อขึ้นระบบจริงแล้วให้ replace ตัวเลขด้วยข้อมูลอัปเดต ณ เวลานั้น

---

## สาระสำคัญสำหรับผู้บริหาร (Executive Summary)

1. เขตสุขภาพที่ 1 มีประชากรรวม **4,089,430** คน (HDC ปี 2569) และมีบุคลากรหลักในฐานข้อมูลปัจจุบัน (filled positions) รวม **แพทย์ 2,764**, **พยาบาลวิชาชีพ 11,113**, **เภสัชกร 1,149** (ที่มาจาก `hr_blueprint.db`).
2. ภาพ “ความไม่สมดุลรายจังหวัด” ชัดเจน โดยจังหวัดภูมิประเทศยาก/ชายแดนมีความเสี่ยงสูงกว่าในเชิง vacancy + ความเปราะบางด้านการทดแทนกำลังคน (retirement risk).
3. ใน 5 ปี (ถึง 4 พฤษภาคม 2574) มีสัญญาณการเกษียณใน 3 วิชาชีพหลักรวม **แพทย์ 95**, **พยาบาลวิชาชีพ 860**, **เภสัชกร 47** (เฉพาะที่ match 3 วิชาชีพหลัก) และรวมทุกตำแหน่ง **2,945** คน.
4. เพื่อใช้กำหนด “ทิศทาง/เป้าหมาย” เบื้องต้นแบบทำได้จริง เอกสารนี้เสนอเป้าหมาย 2 ชั้น:
   - **เป้าหมายขั้นต่ำ 5 ปี (Floor):** ทำให้ทุกจังหวัด “ไม่ต่ำกว่ามัธยฐานของเขต” (Region-1 median) สำหรับแพทย์/พยาบาล/เภสัชฯ ควบคู่กับแผนทดแทนการเกษียณ
   - **เป้าหมาย 10 ปี (Stretch):** ขยับขึ้นสู่ระดับ **P75 ของเขต** สำหรับจังหวัดที่เป็น hub และปรับกลไกยืดหยุ่นเพื่อพื้นที่ยาก (ไม่ใช้หัวคนอย่างเดียว)
5. ข้อจำกัดสำคัญ: ตัวเลขนี้สะท้อน “กำลังคนที่มีในฐานข้อมูล” และ “อัตรากำลัง vacant ในฐานข้อมูล” ยังไม่เท่ากับการสรุปมาตรฐาน FTE ขั้นสุดท้าย จึงใช้เพื่อกำหนดทิศทางและทำ scenario ก่อน แล้วค่อยยืนยัน final standard เมื่อมีข้อมูล workload/บริการจริงครบ

---

## แหล่งข้อมูลจริงที่ใช้ (Data Used)

- ประชากรรายจังหวัด: ตาราง HDC ปี 2569 ในไฟล์ `health_metrics_analysis_report.md`
- กำลังคนและอัตรากำลัง: `hr_blueprint.db` (ตาราง `assignment`, `position`, `personnel`, `organizational_unit`, `provinces`)
- ผลการคำนวณ baseline รายจังหวัด: `output/hr_blueprint_provincial_baseline_region1.json` (generate โดย `scripts/generate_region1_provincial_hr_baseline.py`)

นิยามที่ใช้ในรายงาน:
- **filled** = ตำแหน่งที่มีคนทำงานอยู่ (position_status='filled')
- **vacant** = ตำแหน่งว่างในกรอบ (position_status='vacant')
- **retire_5y** = retirement_date <= 4 พฤษภาคม 2574 (คำนวณจาก study date ที่ pin ไว้ 4 พฤษภาคม 2569)
- **อัตราต่อ 10,000 ประชากร** = headcount / population * 10,000

---

# วิธีคำนวณและตรวจสอบย้อนกลับ (Supervisor Traceability)

ส่วนนี้อธิบายว่า “แต่ละตัวเลขในรายงาน” มาจากไหน คำนวณอย่างไร และ supervisor ตรวจซ้ำได้อย่างไร (reproducible)

## A) ประชากรรายจังหวัด (Population)

**ที่มา:** `health_metrics_analysis_report.md` (ตาราง HDC ปี 2569)  
**วิธีดึง:** สคริปต์อ่านตาราง Markdown แล้ว parse แถวรูปแบบ

`| **<จังหวัด>** | <ชาย> | <หญิง> | **<รวม>** |`

**ตัวเลขที่ใช้ในตารางรายงาน:**
- `population` ใน `output/hr_blueprint_provincial_baseline_region1.json`

## B) จำนวน “กำลังคน” (filled headcount) ใน 3 วิชาชีพหลัก

**ที่มา:** `hr_blueprint.db`  
**นิยามที่ใช้ในงานนี้:** นับจำนวน assignment ที่ active และผูกกับ position ที่ status = filled โดย group ตามจังหวัดของหน่วยบริการ

**วิชาชีพที่นับ (position_name_th):**
- แพทย์ = `นายแพทย์`
- พยาบาลวิชาชีพ = `พยาบาลวิชาชีพ`
- เภสัชกร = `เภสัชกร`

**SQL logic (สรุป):**
```sql
select
  ou.province_code,
  p.position_name_th,
  count(*) as n
from assignment a
join position p on p.position_id = a.position_id
join organizational_unit ou on ou.unit_id = a.unit_id
where a.status = 'active'
  and p.position_status = 'filled'
  and p.position_name_th in ('นายแพทย์','พยาบาลวิชาชีพ','เภสัชกร')
group by ou.province_code, p.position_name_th;
```

**ตัวเลขที่ใช้ในตารางรายงาน:**
- `doctor`, `nurse`, `pharmacist` ใน JSON output

## C) อัตราต่อ 10,000 ประชากร (per 10k)

**สูตร:**
- `rate_per10k = headcount / population * 10,000`

**ตัวเลขที่ใช้ในตารางรายงาน:**
- `doctor_per10k`, `nurse_per10k`, `pharmacist_per10k`

## D) Vacancy (ตำแหน่งว่าง)

**ที่มา:** `hr_blueprint.db` ตาราง `position` + `organizational_unit`  
**นิยาม:** นับตำแหน่งที่ `position_status='vacant'` แล้ว group ตามจังหวัดของ unit นั้น

**SQL logic (vacant ทั้งหมด):**
```sql
select
  ou.province_code,
  count(*) as vacant_all
from position p
join organizational_unit ou on ou.unit_id = p.unit_id
where p.position_status = 'vacant'
group by ou.province_code;
```

**SQL logic (vacant เฉพาะ 3 วิชาชีพหลัก):**
```sql
select
  ou.province_code,
  p.position_name_th,
  count(*) as vacant_n
from position p
join organizational_unit ou on ou.unit_id = p.unit_id
where p.position_status = 'vacant'
  and p.position_name_th in ('นายแพทย์','พยาบาลวิชาชีพ','เภสัชกร')
group by ou.province_code, p.position_name_th;
```

**ตัวเลขที่ใช้ในรายงาน:**
- `vacant_all`, `vacant_doctor`, `vacant_nurse`, `vacant_pharmacist`

## E) Retirement Risk ภายใน 5 ปี (retire_5y)

**ที่มา:** `hr_blueprint.db` ตาราง `personnel` + join `assignment` + join `organizational_unit`  
**นิยาม:** บุคลากรที่มี `retirement_date` และ `retirement_date <= 4 พฤษภาคม 2574` และเป็น active

**SQL logic (รวมทุกตำแหน่ง):**
```sql
select
  ou.province_code,
  count(*) as retire_5y_all
from personnel per
join assignment a on a.personnel_id = per.personnel_id
join organizational_unit ou on ou.unit_id = a.unit_id
where per.retirement_date is not null
  and date(per.retirement_date) <= date('2031-05-04')
  and per.is_active = 1
  and a.status = 'active'
group by ou.province_code;
```

**SQL logic (แยก 3 วิชาชีพหลัก):**
```sql
select
  ou.province_code,
  p.position_name_th,
  count(*) as retire_5y_n
from personnel per
join assignment a on a.personnel_id = per.personnel_id
join position p on p.position_id = a.position_id
join organizational_unit ou on ou.unit_id = a.unit_id
where per.retirement_date is not null
  and date(per.retirement_date) <= date('2031-05-04')
  and per.is_active = 1
  and a.status = 'active'
  and p.position_status = 'filled'
  and p.position_name_th in ('นายแพทย์','พยาบาลวิชาชีพ','เภสัชกร')
group by ou.province_code, p.position_name_th;
```

**ตัวเลขที่ใช้ในรายงาน:**
- `retire_5y_all`, `retire_5y_doctor`, `retire_5y_nurse`, `retire_5y_pharmacist`

## F) Benchmark ภายในเขต: Median และ P75

**ที่มา:** คำนวณจากค่า per10k ของ “ทั้ง 8 จังหวัด” ในเขตสุขภาพที่ 1  
**Median:** ใช้ median ปกติของชุดข้อมูล 8 ค่า  
**P75:** ใช้วิธี nearest-rank (ชุดเล็ก N=8):
- เรียงค่าจากน้อยไปมาก แล้วเลือกตำแหน่ง `ceil(0.75*N)`

**ตัวเลข benchmark อยู่ใน:**
- `benchmarks_per10k` ใน JSON output

## G) Target / GAP (floor: median, stretch: P75)

นิยาม GAP ในรายงานนี้เป็น “จำนวน headcount เพิ่ม (ถ้าติดลบถือว่า 0 ในตารางสรุป)” เพื่อให้จังหวัด “ขึ้นถึง benchmark ต่อ 10k” ตามประชากรของจังหวัดนั้น

**สูตร target headcount:**
- `target = ceil(benchmark_rate_per10k * population / 10,000)`

**สูตร GAP:**
- `gap = target - current_headcount`

**ตัวเลขที่ใช้ในรายงาน:**
- `*_target_median`, `*_gap_median`
- `*_target_p75`, `*_gap_p75`

## H) วิธี supervisor ตรวจซ้ำ (Reproducibility Checklist)

1. ตรวจ population ตั้งต้น: เปิด `health_metrics_analysis_report.md` แล้วดูตาราง HDC ปี 2569 (8 จังหวัด)
2. รันสคริปต์คำนวณใหม่:
   - `python scripts/generate_region1_provincial_hr_baseline.py`
3. เปิดผลลัพธ์:
   - `output/hr_blueprint_provincial_baseline_region1.json`
4. ตรวจว่าใน JSON:
   - `rows` มี 8 แถว (8 จังหวัด)
   - `population` ตรงกับ HDC table
   - ผลรวม `doctor/nurse/pharmacist` รวมทั้งเขตตรงกับสรุปใน Executive Summary
5. หากต้องการตรวจเชิงลึก ให้ run SQL ตัวอย่างในหัวข้อ B–E กับ `hr_blueprint.db`

---

---

# ผลการศึกษา “ตามบทบาทและหน้าที่” ของคณะกรรมการ HR Blueprint (6 ข้อ)

## 1) วิเคราะห์สถานการณ์กำลังคนและความต้องการบริการด้านสุขภาพเชิงพื้นที่ (ภาพรายจังหวัด)

### 1.1 ภาพประชากร (ฐานตั้งต้นความต้องการบริการ)

| จังหวัด | ประชากร (คน) |
|:--|--:|
| เชียงใหม่ | 1,170,694 |
| เชียงราย | 884,137 |
| ลำปาง | 501,176 |
| น่าน | 350,346 |
| พะเยา | 350,078 |
| ลำพูน | 337,453 |
| แพร่ | 315,663 |
| แม่ฮ่องสอน | 179,883 |
| **รวมเขตสุขภาพที่ 1** | **4,089,430** |

### 1.2 ภาพกำลังคนหลัก (แพทย์/พยาบาลวิชาชีพ/เภสัชกร) ต่อ 10,000 ประชากร

| จังหวัด | แพทย์ (คน) | แพทย์/10k | พยาบาลวิชาชีพ (คน) | พยาบาล/10k | เภสัชกร (คน) | เภสัชฯ/10k |
|:--|--:|--:|--:|--:|--:|--:|
| เชียงใหม่ | 761 | 6.50 | 2,815 | 24.05 | 287 | 2.45 |
| เชียงราย | 546 | 6.18 | 2,222 | 25.13 | 231 | 2.61 |
| ลำปาง | 432 | 8.62 | 1,616 | 32.24 | 163 | 3.25 |
| น่าน | 284 | 8.11 | 1,209 | 34.51 | 123 | 3.51 |
| พะเยา | 219 | 6.26 | 1,008 | 28.79 | 107 | 3.06 |
| ลำพูน | 197 | 5.84 | 827 | 24.51 | 89 | 2.64 |
| แพร่ | 229 | 7.25 | 921 | 29.18 | 100 | 3.17 |
| แม่ฮ่องสอน | 96 | 5.34 | 495 | 27.52 | 49 | 2.72 |
| **รวมเขตสุขภาพที่ 1** | **2,764** | **6.76** | **11,113** | **27.17** | **1,149** | **2.81** |

**ข้อค้นพบสำคัญ (จากข้อมูลจริงที่มี):**
- ความแตกต่างเชิงพื้นที่เด่นชัด: จังหวัด “ภูเขา/ชายแดน” มีทั้ง risk จาก vacancy และการคงอยู่ของบุคลากร (retention) สูงกว่าโดยธรรมชาติของพื้นที่
- “เชียงใหม่” แม้เป็น hub แต่มีอัตราพยาบาล/10k ต่ำเมื่อเทียบกับหลายจังหวัดในเขต ซึ่งสะท้อนแรงกดดันด้าน workload ในเมือง/ศูนย์รับส่งต่อ (ต้องใช้ workload จริงมายืนยันในเฟสขึ้นระบบ)

---

## 2) จัดทำและพัฒนาแผนอัตรากำลัง 5 ปี และ 10 ปี แบบบูรณาการทั้งเขต (กรอบเป้าหมายที่ทำได้จริง ณ วันนี้)

เอกสารนี้เสนอ “กรอบตั้งเป้า” ที่ใช้ได้ทันที โดยไม่อ้างว่าเป็นมาตรฐานสุดท้าย

### 2.1 Benchmark ภายในเขต (ใช้ตั้งเป้าขั้นต่ำ/ขั้นยกระดับ)

- มัธยฐานของเขต (Region-1 median; ต่อ 10,000 ประชากร)
  - แพทย์: **6.38**
  - พยาบาลวิชาชีพ: **28.16**
  - เภสัชกร: **2.89**
- P75 ของเขต (ต่อ 10,000 ประชากร)
  - แพทย์: **7.25**
  - พยาบาลวิชาชีพ: **29.18**
  - เภสัชกร: **3.17**

### 2.2 เป้าหมายขั้นต่ำระยะ 5 ปี (Floor)

หลักการ: “ทุกจังหวัดไม่ควรต่ำกว่ามัธยฐานของเขต” ใน 3 วิชาชีพหลัก (เป็น baseline เพื่อความเป็นธรรมเชิงพื้นที่)

ช่องว่าง (gap) ต่อการขึ้นถึง **Region-1 median**:

| จังหวัด | แพทย์ gap ถึง median | พยาบาล gap ถึง median | เภสัชฯ gap ถึง median |
|:--|--:|--:|--:|
| เชียงใหม่ | 0 | +482 | +52 |
| เชียงราย | +18 | +268 | +25 |
| ลำปาง | 0 | 0 | 0 |
| น่าน | 0 | 0 | 0 |
| พะเยา | +5 | 0 | 0 |
| ลำพูน | +19 | +124 | +9 |
| แพร่ | 0 | 0 | 0 |
| แม่ฮ่องสอน | +19 | +12 | +3 |
| **รวม (เฉพาะจังหวัดที่ต่ำกว่า median)** | **+61** | **+886** | **+89** |

หมายเหตุ:
- ตารางนี้ “ไม่เสนอให้ลด” จังหวัดที่สูงกว่า median แต่ใช้เป็นสัญญาณว่าจังหวัดนั้นสามารถทำหน้าที่ **hub/mentor/support** ในระบบเครือข่ายได้
- จำนวน gap เป็น “ตัวตั้งต้น” เพื่อกำหนดทิศทางและวาง scenario ยังไม่ใช่กรอบอัตรากำลังสุดท้าย (ต้องผูกกับ workload จริง, skill mix, และ flow การส่งต่อในเฟสระบบจริง)

### 2.3 เป้าหมายระยะ 10 ปี (Stretch)

หลักการ: “จังหวัดศูนย์กลาง” ขยับขึ้นสู่ P75 และ “พื้นที่ยาก/ชายแดน” ใช้ผสมมาตรการยืดหยุ่น (ไม่ใช่ headcount อย่างเดียว)

ช่องว่าง (gap) ต่อการขึ้นถึง **P75 ของเขต** (สรุปเฉพาะจังหวัดที่ต่ำกว่า P75):
- แพทย์: รวม +303 คน
- พยาบาลวิชาชีพ: รวม +1,161 คน
- เภสัชกร: รวม +165 คน

---

## 3) วิเคราะห์ภาระงานจริงและกำหนดความจำเป็นด้านกำลังคนเชิงระบบ (ผลการศึกษา ณ วันนี้ + ช่องที่ต้องเติม)

**สิ่งที่ทำได้จากข้อมูลจริงที่มีอยู่ตอนนี้**
- ยืนยัน baseline “กำลังคนจริง + vacancy + สัญญาณเกษียณ” แบบรายจังหวัดได้ครบ 8 จังหวัด (พร้อมตัวเลขตรวจสอบย้อนกลับได้จากไฟล์ JSON)
- สร้างตรรกะตั้งเป้าแบบ constraint-friendly: ใช้ median/P75 ภายในเขตเป็น “เกณฑ์ขั้นต่ำ/เกณฑ์ยกระดับ” ซึ่งลดความเสี่ยงจากการอ้างมาตรฐานภายนอกที่ยังไม่พร้อม

**สิ่งที่ยังต้องเติมเพื่อให้เป็น ‘ภาระงานจริง’ (workload-based)**
- workload/throughput จริง (OP/IP, ER, referral) รายจังหวัด/ระดับหน่วยบริการ
- case-mix และบทบาท hub/spoke ในการรับส่งต่อ (เพื่อคำนวณ skill mix ที่เหมาะสม)

**ข้อเสนอที่ทำได้ทันที (โดยไม่ต้องรอข้อมูลครบ)**
- ในเฟสขึ้นระบบจริง ให้เพิ่ม workload facts เข้า `hr_blueprint.db` หรือเชื่อม API เพื่อทำ “workload-weighted target” แทน per-population
- ระหว่างรอ ให้ใช้ gap-to-median เป็น “กรอบทิศทาง” เพื่อจัดลำดับจังหวัดนำร่องและจัดสรรทรัพยากรแบบไม่ผิดทาง

---

## 4) บูรณาการแผนอัตรากำลังกับแผนพัฒนาระบบบริการและแผนทรัพยากรของเขต (แนวทางเชื่อมแผนที่ทำได้จริง)

**ข้อค้นพบเชิงระบบ**
- ถ้าตั้งเป้ากำลังคนโดยไม่ผูกกับ flow การส่งต่อ/รูปแบบบริการ จะทำให้ “เติมคนแต่ไม่ลดภาระ” โดยเฉพาะจังหวัด hub และจังหวัดชายแดน

**ข้อเสนอการบูรณาการที่ทำได้ทันที**
- ผูกเป้าหมาย 5 ปี (Floor) กับ “district/province operating model” แบบง่าย:
  - จังหวัด hub (เช่น เชียงใหม่): เน้นปรับ flow การดูแล + productivity tools + nurse staffing เพื่อรองรับงานส่งต่อ
  - จังหวัดชายแดน/ภูเขา (เช่น เชียงราย, แม่ฮ่องสอน): เน้น retention + rotation + telemedicine + skill-mix
- ใช้ vacancy และ retire_5y เป็นตัวตั้งของ “แผนลงทุนดิจิทัล/ที่พัก/สวัสดิการพื้นที่ยาก” เพื่อเพิ่มการคงอยู่ของกำลังคน ไม่ใช่ใช้การบรรจุอย่างเดียว

---

## 5) เสนอรูปแบบการจัดกำลังคนที่ยืดหยุ่นและเหมาะสมกับบริบทเขตสุขภาพ (ข้อเสนอเชิงปฏิบัติ)

ข้อมูลจริงจาก `hr_blueprint.db` บ่งชี้ว่าจังหวัดชายแดนและภูเขามี vacancy ratio สูง (เช่น แม่ฮ่องสอน ~16.6%) จึงควรใช้ “รูปแบบยืดหยุ่น” ร่วมกับการเพิ่มหัวคน

ข้อเสนอที่ทำได้จริงในระดับเขต (ไม่ต้องเปลี่ยน schema):
- ตั้ง “ทีมข้ามจังหวัด (cluster team)” สำหรับแพทย์เฉพาะทาง/เภสัชคลินิก โดยใช้จังหวัดที่เกิน median เป็น hub สนับสนุนจังหวัดที่ต่ำกว่า median
- ใช้ teleconsult/telepharmacy สำหรับพื้นที่ภูเขา/เดินทางยาก เพื่อลดการใช้กำลังคน onsite ในงานที่ทำ remote ได้
- ปรับแผนหมุนเวียน (rotation) และแพ็กเกจ retention สำหรับจังหวัดชายแดน (เชียงราย, แม่ฮ่องสอน) โดยใช้ vacancy + retire_5y เป็นตัวชี้จุดเสี่ยง

---

## 6) จัดทำข้อเสนอเชิงนโยบายและกลไกการขับเคลื่อนการบริหารกำลังคน (เพื่อให้แผนใช้ได้จริงต่อเนื่อง 5–10 ปี)

### 6.1 ข้อเสนอเชิงนโยบาย (Policy Proposals)

1. ยึด “เป้าหมายขั้นต่ำ 5 ปี (Floor)” เป็นสัญญาเชิงผลลัพธ์ระดับเขต: ทุกจังหวัดไม่ต่ำกว่า median ของเขตใน 3 วิชาชีพหลัก (พร้อมแผนทดแทนเกษียณ)
2. ใช้ “จังหวัด hub” เป็นกลไกเพิ่มศักยภาพทั้งเขต (mentoring + shared service) แทนการทำให้ทุกจังหวัดมีโครงสร้างเหมือนกัน
3. จัดแพ็กเกจ retention/rotation สำหรับจังหวัดชายแดนและพื้นที่ยาก โดยใช้ vacancy ratio + retire_5y เป็นตัวชี้จุดเสี่ยง

### 6.2 กลไกขับเคลื่อน/ติดตามผล (Governance & Monitoring)

- รอบติดตามรายไตรมาส (Quarterly):
  - อัปเดต baseline workforce + vacancy + retirement pipeline
  - ทบทวน gap-to-median และปรับ “จังหวัดนำร่อง/มาตรการ” ตามหลักฐาน
- รอบทบทวนรายปี (Annual):
  - เมื่อมี workload facts จริง ให้ปรับ target จาก per-population เป็น workload-weighted
  - สรุป lessons learned เพื่อปรับ “กติกาจัดสรรกำลังคน” และ “งบ/สวัสดิการพื้นที่ยาก”

---

## ภาคผนวก: ตัวชี้วัดความเสี่ยงกำลังคน (Vacancy + Retirement) รายจังหวัด

| จังหวัด | vacant ทั้งหมด (ตำแหน่ง) | เกษียณ <=5 ปี (ทุกตำแหน่ง) | เกษียณ <=5 ปี: แพทย์ | พยาบาล | เภสัชฯ |
|:--|--:|--:|--:|--:|--:|
| เชียงใหม่ | 776 | 613 | 29 | 163 | 16 |
| เชียงราย | 656 | 508 | 14 | 127 | 6 |
| ลำปาง | 654 | 489 | 15 | 149 | 10 |
| น่าน | 283 | 355 | 7 | 96 | 4 |
| พะเยา | 358 | 278 | 8 | 95 | 5 |
| ลำพูน | 212 | 221 | 9 | 73 | 3 |
| แพร่ | 211 | 308 | 11 | 112 | 3 |
| แม่ฮ่องสอน | 304 | 173 | 2 | 45 | 0 |

ไฟล์อ้างอิงผลคำนวณ: `output/hr_blueprint_provincial_baseline_region1.json`
